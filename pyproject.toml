[tool.poetry]
name = "msg-webhook"
version = "0.1.0"
description = "A Telegram WebHook service for message forwarding"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
fastapi = "^0.109.2"
uvicorn = "^0.27.1"
python-telegram-bot = "^20.8"
python-dotenv = "^1.0.1"
pydantic = "^2.6.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api" 